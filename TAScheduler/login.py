class Login(object):
    def __init__(self, name="", password="") -> object:
        # precondition: if provided, name and password and strings, both are at least 4 in length
        # postcondition: self.name is unique and self.password is at least 4 in length
        pass